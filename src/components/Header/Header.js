import React from "react";
import "./style.css";

function Header() {
    return (
        
        <header className="header">
            <h1>Clicky Game</h1>
            <hs>Click on an image to earn points, but don't click on any more than once!</hs>
        </header>

    )
};

export default Header;